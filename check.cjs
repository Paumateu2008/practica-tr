const fs=require('fs');const code=fs.readFileSync('src/App.jsx','utf8');const stack=[];for(let i=0;i<code.length;i++){const ch=code[i];if(ch==='{'||ch==='('||ch==='['){stack.push({ch,pos:i});}else if(ch===')'){const top=stack.pop();if(!top||top.ch!=='('){console.log('mismatch ) at',i);break;}}else if(ch==='}'){const top=stack.pop();if(!top||top.ch!=='{'){console.log('mismatch } at',i);break;}}else if(ch===']'){const top=stack.pop();if(!top||top.ch!=='['){console.log('mismatch ] at',i);break;}}}
console.log('stack length',stack.length);if(stack.length){console.log(stack.map(s=>s.ch).join(''), stack.map(s=>s.pos));}
